/**
 * Orchestration layer entry point. Contracts only for now (v0.2) — the marketplace,
 * exchange, ledger, and supervisor loop land per the ROADMAP. Nothing here executes
 * yet; it defines the shape every future implementation must satisfy.
 */
export * from "./types.js";
